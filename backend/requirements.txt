# ============================================
# SafeHer Backend — Python Dependencies
# ============================================
# Install: pip install -r requirements.txt
# Note: Some packages are large (sentence-transformers ~500MB with torch)
#
# IMPORTANT for Render deployment:
#   - runtime.txt pins Python to 3.12.x
#   - Do NOT pin numpy/pandas/scikit-learn to exact versions — let pip
#     pull the latest version that has prebuilt wheels for Python 3.12.
#     Otherwise pip falls back to compiling from source (10-30 min build).
# ============================================

# === Web Framework ===
fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.32.0,<0.40.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0

# === Language Processing ===
langdetect>=1.0.9

# === RAG & AI ===
sentence-transformers>=2.2.2,<6.0.0
python-multipart>=0.0.6
email-validator>=2.1.0
chromadb>=0.5.23,<2.0.0
google-generativeai>=0.7.2,<1.0.0
groq>=0.11.0,<1.0.0

# === Routing & ML ===
# Use ranges so pip finds wheels for the current Python version.
osmnx>=2.0.0,<3.0.0
networkx>=3.0,<5.0
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
geopandas>=1.0.0
shapely>=2.0.0

# === Database ===
supabase>=2.9.0,<3.0.0
aiosqlite>=0.20.0

# === HTTP ===
httpx>=0.27.0

# === Utilities ===
python-dotenv>=1.0.0